apiVersion: v1
kind: Pod
metadata:
  name: apache2-pod
spec:
  containers:
  - image: manager.web.mate.solutions:5000/apache2:v2
    name: apache2
    volumeMounts:
    - mountPath: /dir-data
      name: data-volume
  volumes:
  - name: data-volume
    hostPath:
      # directory location on host
      path: /dir-data/apache2
  hostNetwork: true
#  - hostPort: 80
#    protocol: TCP
#  - containerPort: 80
#    protocol: TCP
